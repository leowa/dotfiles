# Credit where credit is due:
# http://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/

# NOTE: if you find some weird behavior when trying out new conf,
# try to first kill-server and retry

unbind C-b
# set -g prefix C-a
# # press C-a twice to go to begining of the line
# bind-key C-a send-prefix

# set up new Control key to Caps Lock follow
# https://mutelight.org/caps-lock
# Basically, you need to first download PCKeyboardHack first
# then configure Caps Lock to F10 by launching seil from applications
# Change Caps Lock key to keycode as 109
# Note: in order to for this to work for terminal, you need to set
# Terminalinf as "Declare terminal as xterm-256-color" otherwise,
# function key will be sent as ascii code

# This is a little trick here for new mac, should use "Karabiner-Elements" to modify function key for f10 to f10 instead of mute
set -g prefix F10
#bind-key Escape send-prefix
bind-key F10 last-window

set -g mode-keys vi

# let tmux knows that terminal support utf-8 encoding
# need to set  LC_ALL and LANG environment variables too
# export LC_ALL=en_US.UTF-8
# export LANG=en_US.UTF-8
# or start tmux with -u global option, like `tmux -u new -s test`
# NOTE: for zshell, should export both variables to ~/.zshrc too
# utf-8 option is not supported/needed for new version tmux
#set -g client_utf8 on

# Styles the pane borders
set -g pane-border-fg green
set -g pane-border-bg black

# Styles the active pane border. Helps when you have
# more than two panes.
set -g pane-active-border-fg white
set -g pane-active-border-bg yellow

## {{  Style setting from: https://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/
######################
### DESIGN CHANGES ###
######################

# panes
set -g pane-border-fg black
set -g pane-active-border-fg brightred

## Status bar design
# status line
set -g status-justify left
set -g status-bg default
set -g status-fg colour12
set -g status-interval 2

# messaging
set -g message-fg black
set -g message-bg yellow
set -g message-command-fg blue
set -g message-command-bg black

#window mode
setw -g mode-bg colour6
setw -g mode-fg colour0

# window status
setw -g window-status-format " #F#I:#W#F "
setw -g window-status-current-format " #F#I:#W#F "
setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
setw -g window-status-current-bg colour0
setw -g window-status-current-fg colour11
setw -g window-status-current-attr dim
setw -g window-status-bg green
setw -g window-status-fg black
setw -g window-status-attr reverse


# loud or quiet?
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none

set -g default-terminal "screen-256color"

# The modes {
setw -g clock-mode-colour colour135
setw -g mode-attr bold
setw -g mode-fg colour196
setw -g mode-bg colour238

# }
# The panes {

set -g pane-border-bg colour235
set -g pane-border-fg colour238
set -g pane-active-border-bg colour236
set -g pane-active-border-fg colour51

# }
# The statusbar {

set -g status-position bottom
set -g status-bg colour234
set -g status-fg colour137
set -g status-attr dim
# set -g status-left ''
# [andyz] always show session name on status
set -g status-left '#[fg=colour190,bold][#S-#I]'
set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20

setw -g window-status-current-fg colour81
setw -g window-status-current-bg colour238
setw -g window-status-current-attr bold
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '

setw -g window-status-fg colour138
setw -g window-status-bg colour235
setw -g window-status-attr none
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

setw -g window-status-bell-attr bold
setw -g window-status-bell-fg colour255
setw -g window-status-bell-bg colour1

# }
# The messages {

set -g message-attr bold
set -g message-fg colour232
set -g message-bg colour166

# }

## Style setting from: https://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/ }}


# switch panes using Alt-arrow without prefix
# For macbook make sure you enabled meta key
# using Option key from keyboard setting
bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D

# Enable mouse control
# ! note: this will mess around mouse key in terminal
# and you won't be able to select text uing mouse
# set -g mouse-select-pane on
# set -g mouse-resize-pane on

# don't rename windows automatically
set-option -g allow-rename off

# split panes using | and -
unbind %
unbind '"'
bind \ split-window -h
bind - split-window -v


# clear screen buffer
# bind -n C-k send-keys -R \; clear-history

# don't rename windows automatically
set-option -g allow-rename off


###################### tmux plugins###################
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Install tmux-resurrect to enable saving and restoring tmux sessions
# by simply pressing `prefix + S - save`, and `prefix + R - restore`
set -g @plugin 'tmux-plugins/tmux-resurrect'
#run-shell /Users/andyz/dev/oss/tmux-resurrect/resurrect.tmux
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'
set -g @resurrect-processes 'ssh assh psql mysql sqlite3'

# Install tmux-continuum to save automatically
set -g @plugin 'tmux-plugins/tmux-continuum'
#run-shell /Users/andyz/dev/oss/tmux-resurrect/resurrect.tmux
set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# Install tpm: `git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm`
run '~/.tmux/plugins/tpm/tpm'
